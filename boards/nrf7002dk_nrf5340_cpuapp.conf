#
# Copyright (c) 2025 Nordic Semiconductor ASA
#
# SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
#
# Board-specific overrides for nRF7002DK (nRF5340 + onboard nRF7002 WiFi).
# WiFi is available via the onboard nRF7002 chip (no shield required).
# No nRF5340 Audio DK-specific hardware: no CS47L63, no power measurement,
# no SD card module. Gateway uses USB as audio source by default.

# nRF5340 peripheral drivers required by audio_sync_timer.c and audio_i2s.c
CONFIG_NRFX_I2S=y

# NRFX_TIMER is auto-selected by AUDIO_SYNC_TIMER_USES_RTC via Kconfig

# nRF7002DK has no SD card; disable FAT filesystem to avoid FF_VOLUMES=0 error
# (Kconfig.defaults unconditionally enables FAT_FILESYSTEM_ELM, but with no
# disk DTS nodes the FatFS volume count would be 0, which is invalid)
CONFIG_FAT_FILESYSTEM_ELM=n
