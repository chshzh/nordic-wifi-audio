#
# Copyright (c) 2024 Nordic Semiconductor ASA
#
# SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
#

CONFIG_NRF5340_AUDIO_CS47L63_DRIVER=y
CONFIG_NRF5340_AUDIO_POWER_MEASUREMENT=y
CONFIG_NRF5340_AUDIO_SD_CARD_MODULE=y

# Enable low-level peripherals used directly by the audio stack
CONFIG_NRFX_I2S=y
CONFIG_NRFX_TIMER=y
CONFIG_PINCTRL=y

# zego button/LED brick configuration for nRF5340 Audio DK
# 5 buttons: sw0(VOL-) sw1(VOL+) sw2(PLAY) sw3(BTN4) sw4(BTN5)
CONFIG_ZEGO_BUTTON=y
CONFIG_ZEGO_BUTTON_NUM_BUTTONS=5
CONFIG_ZEGO_BUTTON_BACKEND_DK=y
# 9 LEDs: RGB1(idx 0-2) RGB2(idx 3-5) mono(idx 6-8); ROTATE covers RGB1 by default
CONFIG_ZEGO_LED=y
CONFIG_ZEGO_LED_NUM_LEDS=9
CONFIG_ZEGO_LED_BACKEND_DK=y

# App UX module: use RGB2 (LEDs 3,4,5) for Wi-Fi status ROTATE animation
# and LED 4 (green channel of RGB2) for the connected indicator
CONFIG_APP_UX_ROTATE_FIRST_LED=3
CONFIG_APP_UX_ROTATE_COUNT=3
CONFIG_APP_UX_CONNECTED_LED=4
CONFIG_APP_UX_CONNECTED_LED_GREEN_ONLY=y

# Increase log buffer so the boot banner is not silently overwritten
# before the log processing thread flushes it (default 1024 is too small
# for this app's heavier boot-time log traffic).
CONFIG_LOG_BUFFER_SIZE=8192
